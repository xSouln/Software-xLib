<UserControl x:Class="xLibV100.UI.Views.PortView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:xLibV100.UI.Views"
             mc:Ignorable="d" Width="450" Height="auto">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="150*" MinWidth="160" MaxWidth="160"/>
            <ColumnDefinition Width="254*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="40"/>
            <RowDefinition Height="40"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <ComboBox HorizontalAlignment="Left" Margin="8,3,0,3" FontSize="18" Width="120" Grid.Row="1" Grid.Column="1"/>
        <Label Content="Available ports:" Foreground="#FFDEC316" Margin="5,0,0,0" FontSize="18" Grid.Row="1"/>
        <Button Content="Button" FontSize="18"  Template="{DynamicResource ButtonTemplate1}" HorizontalAlignment="Left" Margin="150,3,0,3" Width="118" Grid.Column="1" Grid.Row="1"/>
        <Label Content="Connection state:" Foreground="#FFDEC316" Margin="5,0,0,0" FontSize="18"/>
    </Grid>
</UserControl>
