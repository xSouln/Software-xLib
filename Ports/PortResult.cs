namespace xLibV100.Ports
{
    public enum PortResult
    {
        Accept,
        Error,

        DataError,
        ConnectionError,
        InternalError,

        NotSupported,
        Busy
    }
}
